{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "deploy": "next build && next start",
    "build": "next build",
    "start": "next start",
    "deploy": "npm run build && npm run start",
    "lint": "next lint",
    "test": "jest",
    "test:ci": "jest --ci"
  }
}
