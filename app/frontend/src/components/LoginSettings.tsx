export default function LoginSettings() {
	return (
        <div className="component">
            <h1>Login settings</h1>
            <p>
                Login name, enable two-factor authentication, change password
            </p>
        </div>
	);
}
